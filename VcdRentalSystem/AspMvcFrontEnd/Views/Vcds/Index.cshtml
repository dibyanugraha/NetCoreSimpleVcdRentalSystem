@model IEnumerable<AspMvcFrontEnd.Models.VcdModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VcdTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VcdBoughtDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VcdTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VcdBoughtDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VcdId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VcdId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VcdId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
